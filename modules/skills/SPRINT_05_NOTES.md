# Sprint 05 Notes

- Fixed SK-1: `_loadSkillFromFolder()` now detects `template.md`, `references/`, and `examples/` before returning.
- Fixed SK-2: chat pre-question substitution uses `substituteVariables()` from `modules/skills/index.js`.
